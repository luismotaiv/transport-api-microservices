spring:
  datasource:
    url: jdbc:mysql://database-1.chgacewiiafi.us-east-2.rds.amazonaws.com:3306/order_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: admin
    password: Atom007x
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8082

# URL del driver-service (clave para comunicación)
services:
  driver:
    url: http://localhost:8081

logging:
  level:
    root: INFO
    com.transport: DEBUG

#SWAGEER
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html