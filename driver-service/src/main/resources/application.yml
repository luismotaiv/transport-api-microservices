spring:
  datasource:
    url: jdbc:mysql://database-1.chgacewiiafi.us-east-2.rds.amazonaws.com:3306/driver_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: admin
    password: Atom007x
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081